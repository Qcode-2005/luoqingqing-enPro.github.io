# Netlify 配置文件

# 构建配置
[build]
  # 构建命令（前端是静态文件，不需要构建）
  command = ""
  # 发布目录（前端文件所在目录）
  publish = "envPro"
  # 函数目录
  functions = "netlify/functions"

# 重定向配置（确保单页应用路由正常工作）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
